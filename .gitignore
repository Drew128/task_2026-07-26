# Raw source data (lives in GCS; not committed)
data/

# dbt artifacts
target/
dbt_packages/
logs/
.user.yml

# Secrets — never commit credentials / service-account keys
profiles.yml
**/profiles.yml
*keyfile*.json
*-key.json
*service-account*.json

# OS
.DS_Store
